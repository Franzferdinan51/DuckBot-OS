DANGEROUSLY_DISABLE_HOST_CHECK=true
HOST=localhost
PORT=3000
HTTPS=false
BROWSER=none

# Fixed configuration for React Scripts v5+ compatibility
# Disabled host check to avoid allowedHosts issues
# BROWSER=none prevents auto-opening browser for Electron use
